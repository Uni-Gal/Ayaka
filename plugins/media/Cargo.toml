cargo-features = ["per-package-target"]

[package]
name = "media"
version = "0.1.0"
edition = "2021"
forced-target = "wasm32-wasi"

[lib]
crate-type = ["cdylib"]

[dependencies]
gal-bindings = { path = "../../utils/gal-bindings" }

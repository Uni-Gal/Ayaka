[package]
name = "random"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
gal-bindings = { path = "../../gal-bindings" }
rand = { version = "0.8", default-features = false, features = ["std_rng"] }
lazy_static = "1.4"
